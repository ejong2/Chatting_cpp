#pragma once

#include <iostream>

#include "PacketEnums.h"
#include "PacketStructs.h"

typedef unsigned char BYTE;

class PacketHandler
{
public:
    PacketHandler() {}
public:

public:
    // 회원가입 요쳥 패킷을 만드는 메서드

    // 로그인 요청 패킷을 만드는 메서드

    // 로그아웃 요청 패킷을 만드는 메서드
};


